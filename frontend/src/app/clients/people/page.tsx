import { ClientsWorkspacePlaceholder } from "@/components/clients/ClientsWorkspacePlaceholder";

export default function ClientPeoplePage() {
  return (
    <ClientsWorkspacePlaceholder
      title="People"
      summary="This route prepares the new people workspace in a Twenty-inspired pattern: a persistent list on the left and contextual detail on the right."
      description="In the next phase, this screen will evolve into the CRM shell with filters, a structured table, and a side detail panel."
    />
  );
}
